transformers>=4.42.0
accelerate>=0.31.0
torch --extra-index-url https://download.pytorch.org/whl/cpu
safetensors
gradio>=4.38.0
bitsandbytes; platform_system != 'Windows'  # optional quantization (not yet stable on Windows CPU)
python-dotenv>=1.0.0
